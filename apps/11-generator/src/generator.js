/**
 * TODO: écrivez une fonction génératrice
 * @name iterator#generatorFunction
 * @type {function}
 * @param {number} x
 * @returns {number} final result = 13
 */
export function generatorFunction() {
  let finalResult;
  let fib = [];
  fib[0] = 0;
  fib[1] = 1;

  return finalResult;
}
