{
  "root": "apps/06-destructuring",
  "projectType": "application",
  "targets": {
    "serve": {
      "executor": "@nrwl/workspace:run-commands",
      "options": {
        "command": "vite",
        "cwd": "apps/06-destructuring"
      }
    },
    "test": {
      "executor": "@nrwl/workspace:run-commands",
      "options": {
        "command": "vitest",
        "cwd": "apps/06-destructuring"
      }
    }
  }
}
